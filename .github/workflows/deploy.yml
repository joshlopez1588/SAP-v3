name: Railway Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy handled by Railway GitHub integration
        run: |
          echo "Railway auto-deploys when this repo is linked in Railway project settings."
          echo "DB migrations run automatically on container startup via backend/entrypoint.sh."
